# Hacker Tools - Environment Variables
# Copy this file to `.env` and fill in your actual values
# Never commit `.env` to version control (it's in .gitignore)

# =============================================================================
# Required for Development/Testing
# =============================================================================

# RubyLLM API Key
# Get your API key from: https://platform.openai.com/api-keys (for OpenAI)
# Or use your preferred LLM provider's API key
OPENAI_API_KEY=your_openai_api_key_here

# =============================================================================
# Optional - Email Configuration
# =============================================================================

# Devise Mailer Sender Address
# The email address that appears as the sender for Devise emails (password reset, etc.)
# Should match your verified domain in MailerToGo (production) or use any email (development)
# Default fallback: noreply@hackertools.site
DEVISE_MAILER_SENDER=noreply@hackertools.site

# =============================================================================
# Optional - Active Storage (Cloudinary)
# =============================================================================

# Cloudinary URL for file uploads
# Format: cloudinary://api_key:api_secret@cloud_name
# Only needed if using Cloudinary for Active Storage in development
# In production, set this via Heroku config vars
CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name

# =============================================================================
# Production Only (Auto-configured on Heroku)
# =============================================================================

# These are automatically set by the MailerToGo Heroku add-on
# You don't need to set these locally - they're documented here for reference
# MAILERTOGO_SMTP_HOST=smtp.us-west-1.mailertogo.net
# MAILERTOGO_SMTP_PORT=587
# MAILERTOGO_SMTP_USER=your_smtp_username
# MAILERTOGO_SMTP_PASSWORD=your_smtp_password
# MAILERTOGO_DOMAIN=www.hackertools.site

# =============================================================================
# Database (Optional - Rails uses config/database.yml by default)
# =============================================================================

# If you want to override database configuration via environment variable:
# DATABASE_URL=postgres://username:password@localhost/hacker_tools_development

# =============================================================================
# Rails Master Key (Production)
# =============================================================================

# For production, set this to decrypt credentials.yml.enc
# Get from: config/master.key (development) or Heroku config (production)
# RAILS_MASTER_KEY=your_master_key_here
